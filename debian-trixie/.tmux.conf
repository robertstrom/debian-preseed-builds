# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# quick pane cycling
# unbind ^A
# bind ^A select-pane -t :.+

# remap prefix from 'C-a' to 'C-b'
# unbind C-a
# set-option -g prefix C-b
# bind-key C-b send-prefix

# always load the reset file first
# source-file ~/.tmux.reset.conf

# Set C-r bind to reload the Tmux configuration
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# I believe that the tmux-sensible plugin takes care of this
# Increase history limit from 2000 to 5000
# set-option -g history-limit 5000

# Begin mouse support section
# This is the best resource that I have found for enabling good mouse support / behavior in tmux
# https://www.baeldung.com/linux/tmux-copy-paste-keyboard-mouse
set -g mouse on
bind -n WheelUpPane if-shell -F -t = "{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
bind -n WheelDownPane select-pane -t= \; send-keys -M
bind -n C-WheelUpPane select-pane -t= \; copy-mode -e \; send-keys -M
bind -T copy-mode-vi    C-WheelUpPane   send-keys -X halfpage-up
bind -T copy-mode-vi    C-WheelDownPane send-keys -X halfpage-down
bind -T copy-mode-emacs C-WheelUpPane   send-keys -X halfpage-up
bind -T copy-mode-emacs C-WheelDownPane send-keys -X halfpage-down

setw -g mode-keys vi
unbind -T copy-mode-vi Enter# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# quick pane cycling
# unbind ^A
# bind ^A select-pane -t :.+

# remap prefix from 'C-a' to 'C-b'
# unbind C-a
# set-option -g prefix C-b
# bind-key C-b send-prefix

# always load the reset file first
# source-file ~/.tmux.reset.conf

# Set C-r bind to reload the Tmux configuration
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# I believe that the tmux-sensible plugin takes care of this
# Increase history limit from 2000 to 5000
# set-option -g history-limit 5000

# Begin mouse support section
# This is the best resource that I have found for enabling good mouse support / behavior in tmux
# https://www.baeldung.com/linux/tmux-copy-paste-keyboard-mouse
set -g mouse on
bind -n WheelUpPane if-shell -F -t = "{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
bind -n WheelDownPane select-pane -t= \; send-keys -M
bind -n C-WheelUpPane select-pane -t= \; copy-mode -e \; send-keys -M
bind -T copy-mode-vi    C-WheelUpPane   send-keys -X halfpage-up
bind -T copy-mode-vi    C-WheelDownPane send-keys -X halfpage-down
bind -T copy-mode-emacs C-WheelUpPane   send-keys -X halfpage-up
bind -T copy-mode-emacs C-WheelDownPane send-keys -X halfpage-down

setw -g mode-keys vi
unbind -T copy-mode-vi Enter
bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "xclip -selection c"
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xclip -in -selection clipboard"
# End mouse support section

# named-snapshot key bindings
# set -g @named-snapshot-save 'C-m:manual M:* C-d:dev'
# set -g @named-snapshot-restore 'C-n:manual N:* D:dev'



# Window Title
set-option -g automatic-rename on
set-option -g set-titles on
set-option -g set-titles-string "#S #I :#P #W:#T"

# Place status bar on the top
set-option -g status-position top

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'jaclu/tmux-menus'
set -g @plugin 'tmux-plugins/tmux-logging'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin '2kabhishek/tmux2k'
# set -g @plugin 'tmux-plugins/tmux-sidebar'
# information on shutting down and reloading saved tmux sessions
# https://github.com/tmux-plugins/tmux-resurrect/issues/120
set -g @plugin 'tmux-plugins/tmux-resurrect'
# https://github.com/laktak/tome
# set -g @plugin 'laktak/tome'
# https://github.com/spywhere/tmux-named-snapshot
# set -g @plugin 'spywhere/tmux-named-snapshot'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "xclip -selection c"
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "xclip -in -selection clipboard"
# End mouse support section

# named-snapshot key bindings
# set -g @named-snapshot-save 'C-m:manual M:* C-d:dev'
# set -g @named-snapshot-restore 'C-n:manual N:* D:dev'



# Window Title
set-option -g automatic-rename on
set-option -g set-titles on
set-option -g set-titles-string "#S #I :#P #W:#T"

# Place status bar on the top
set-option -g status-position top

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'jaclu/tmux-menus'
set -g @plugin 'tmux-plugins/tmux-logging'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin '2kabhishek/tmux2k'
# set -g @plugin 'tmux-plugins/tmux-sidebar'
# information on shutting down and reloading saved tmux sessions
# https://github.com/tmux-plugins/tmux-resurrect/issues/120
set -g @plugin 'tmux-plugins/tmux-resurrect'
# https://github.com/laktak/tome
# set -g @plugin 'laktak/tome'
# https://github.com/spywhere/tmux-named-snapshot
# set -g @plugin 'spywhere/tmux-named-snapshot'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

